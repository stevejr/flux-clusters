apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - metabase.yaml

patches:
- target:
      kind: GitRepository
      name: metabase
      namespace: flux-system
  patch: |-
    - op: replace
      path: /spec/ref/tag
      value: v5.1.0
